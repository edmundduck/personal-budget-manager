<ul>
    <%
        if (confirm_msg) {
            if (Array.isArray(confirm_msg)) {
                confirm_msg.forEach((msg) => {
    %>
                    <li class="confirm_msg"><%= msg %></li>
    <%
                });
            } else {
    %>
                <li class="confirm_msg"><%= confirm_msg %></li>
    <%
            }
        };
    %>
</ul>